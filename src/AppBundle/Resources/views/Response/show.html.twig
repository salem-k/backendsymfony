{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Response</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Content</th>
                <td>{{ entity.content }}</td>
            </tr>
            <tr>
                <th>Custom</th>
                <td>{{ entity.custom }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('response') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('response_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
