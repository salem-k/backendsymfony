{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Response list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Content</th>
                <th>Custom</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('response_show', { 'id': entity.id }) }}">{{ entity.content }}</a></td>
                <td>{{ entity.custom }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('response_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('response_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('response_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
