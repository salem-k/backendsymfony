{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Quizz edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('homepage') }}">
            Back
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
