{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Quizz creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('quizz') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
