{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ResponseQuizz</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Custom</th>
                <td>{{ entity.custom }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('responsequizz') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('responsequizz_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
