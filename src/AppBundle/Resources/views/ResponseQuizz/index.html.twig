{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ResponseQuizz list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Custom</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('responsequizz_show', { 'id': entity.id }) }}">{{ entity.custom }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('responsequizz_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('responsequizz_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('responsequizz_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
